---
import { SITE_TITLE, SOCIAL } from '../consts';
import Base from '../layouts/Base.astro';
---

<Base title={`关于 · ${SITE_TITLE}`} description="关于我和这个站点。">
	<section class="shell">
		<header class="page-head">
			<div class="eyebrow rise"><b>Crew</b> About · 船员</div>
			<h1 class="section-title rise d1">关于，<span>这艘船和掌舵的人。</span></h1>
		</header>
		<div class="about-body prose rise d2">
			<!-- TODO: 写你自己的介绍 -->
			<p>这里是占位文字，写点你是谁、在做什么、这个博客打算写什么。</p>

			<h2>联系</h2>
			<ul>
				{SOCIAL.github && <li><a href={SOCIAL.github}>GitHub</a></li>}
				{SOCIAL.email && <li><a href={`mailto:${SOCIAL.email}`}>{SOCIAL.email}</a></li>}
				<li><a href="/rss.xml">RSS 订阅</a></li>
			</ul>

			<h2>这个站</h2>
			<p>
				Astro 构建，静态输出，托管在 Cloudflare Workers 上。内容以 Markdown 形式写在仓库里，push 之后自动部署。
			</p>
		</div>
	</section>
</Base>
